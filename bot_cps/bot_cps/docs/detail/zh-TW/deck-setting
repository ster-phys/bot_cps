設定 `/deck` 指令中各項參數的指令．```
/deck-setting
```
輸入以上指令，將會顯示下列按鈕：

有顏色的按鈕代表選擇中的條件，灰色按鈕代表**未選擇**的條件．可以透過點擊按鈕選擇/解除選擇．
選擇 `[均衡]` 的狀況下，將會生成較為平衡的卡組．選擇 `[隨機]` 的狀況下，將生成完全隨機的卡組．
點擊 `[初始化]` 將會回到初始設定．
點擊 `[執行]` 將生成卡組．也能夠使用 `/deck` 指令直接生成．
