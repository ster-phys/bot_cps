コンパスのカード名（略称でも可）を入力することで，そのカードの詳細を出力するコマンド．
[やぎシミュ](http://yagitools.html.xdomain.jp/compas-deck/)様のデータを使用しています．

`cards` 引数に，カードの名前を空白区切りで入力する必要がある．
カードの名前が１つの場合はそのカードの詳細が，複数（最大４つ）の場合はデッキとしてのパラメータが表示される．

`level` 引数で，カードのレベルを設定することができる．
カードのレベルは，出力結果と共に出力されるボタンを押すことでも，変更できる．
特に，複数のカードを入力して，デッキとしてのパラメータが表示されている場合は，次のボタンが表示される．

`[＜]`：左に移動する
`[−]`：選択中のカードのレベルを10下げる
`[＋]`：選択中のカードのレベルを10上げる
`[＞]`：右に移動する

現在選択されているカードは，右下の塗りつぶされた四角で確認できる（□：未選択，■：選択中）．
